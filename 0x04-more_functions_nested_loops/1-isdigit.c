#include "main.h"
/**
* _isdigit - check for a digit (0 through 9)
*
* Return: Always 0.
*/
int _isdigit(int c)
{
char c;
c = '0';
printf("%c: %d\n", c, _isdigit(c));
c = 'a';
printf("%c: %d\n", c, _isdigit(c));
{
return (0);
}}
